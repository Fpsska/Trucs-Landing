// let fields = document.querySelectorAll('.input__element');
// Array.prototype.forEach.call(fields, function (input) {
//     let label = input.nextElementSibling,
//         labelVal = label.querySelector('.input__title').innerText;

//     input.addEventListener('change', function () {
//         let countFiles = '';
//         if (this.files && this.files.length >= 1)
//             countFiles = this.files.length;

//         if (countFiles)
//             label.querySelector('.input__title').innerText = 'Выбрано файлов: ' + countFiles;
//         else
//             label.querySelector('.input__title').innerText = labelVal;
//     });
// });
